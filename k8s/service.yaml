apiVersion: v1
kind: Service

metadata:
  name: construction-service

spec:
  type: NodePort

  selector:
    app: construction-api

  ports:
    - port: 80
      targetPort: 8080
      nodePort: 30007